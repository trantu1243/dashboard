<%- include('../components/header/table-header') %>

<div class="wrapper">
  
    <%- include('../components/sidebar/sidebar') %>

  <!-- Content Wrapper. Contains page content -->
  <div class="content-wrapper">
    <!-- Content Header (Page header) -->
    <section class="content-header">
      <div class="container-fluid">
        <div class="row mb-2">
          <div class="col-sm-6">
            <h1>Đội</h1>
          </div>
         
        </div>
      </div><!-- /.container-fluid -->
    </section>

    <!-- Main content -->
    <section class="content">
        <div class="container-fluid">
            <div class="row">
                <div class="col-12">
                    <div class="card card-primary">
                        <div class="card-header">
                          <h3 class="card-title">Add</h3>
                        </div>
                       
                        <form action=<%= `/team/edit/${team._id}` %> method="POST">
                            
                            <div class="card-body">
                                <div class="form-group">
                                    <label for="service_id">Service id</label>
                                    <input name="service_id" value="<%= team.service_id %>" type="text" class="form-control" id="service_id" placeholder="Nhập" autocomplete="none" disabled>
                                </div>

                                <div class="form-group">
                                    <label for="name">Tên</label>
                                    <input name="name" value="<%= team.name %>" type="text" class="form-control" id="name" placeholder="Nhập tên" autocomplete="none" disabled>
                                </div>
                                
                                
                                <div class="form-group">
                                    <label for="token">Token</label>
                                    <input name="token" value="<%= team.token ? team.token : '' %>" type="text" class="form-control" id="token" placeholder="" autocomplete="none" disabled>
                                </div>

                            </div>
    
                            <div class="card-footer">
                                <button type="submit" class="btn btn-primary">Generate token</button>
                            </div>
                        </form>
                        <!-- /.card-body -->
                      </div>
                    <!-- /.card -->
                </div>
            </div>
        </div><!--/. container-fluid -->
      <!-- /.container-fluid -->
    </section>
    <!-- /.content -->
  </div>
  <!-- /.content-wrapper -->
  <footer class="main-footer">
    <div class="float-right d-none d-sm-block">
    </div>
  </footer>

  <!-- Control Sidebar -->
  <aside class="control-sidebar control-sidebar-dark">
    <!-- Control sidebar content goes here -->
  </aside>
  <!-- /.control-sidebar -->
</div>
<!-- ./wrapper -->

<%- include('../components/footer/table-footer') %>
